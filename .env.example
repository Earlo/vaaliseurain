PORT=3000
# Set this to enable authenticated operator updates.
DASHBOARD_EDIT_TOKEN=change-me
# Optional writable location for runtime snapshot overrides.
# VaaliSeurain_DATA_DIR=/var/lib/VaaliSeurain
# Automatic source refresh is enabled by default and runs every five minutes.
# SOURCE_REFRESH_ENABLED=false
# SOURCE_REFRESH_INTERVAL_MS=300000
